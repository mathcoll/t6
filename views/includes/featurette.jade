.col-xs-12.col-sm-6.col-lg-3
  .box
    .icon
      .image
        i(class="#{featurette.class}")
      .info
        h3.title #{featurette.title}
          small  #{featurette.subtitle}
        p
          | #{featurette.description}
        .more
          a(href='#{featurette.link}', role='button', title='#{featurette.title}')
            | View details 
            i.fa.fa-angle-double-right
    .space
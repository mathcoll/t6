-if ( list != null )
  p Email 
    strong #{mail} 
    | have successfully been removed from the list 
    strong #{list}
    | .
  p Your subscriptions:
    ul
      -if ( user && user.unsubscription.reminder > 0 )
        li Reminder Welcome email - 
          a(href="../../../subscribe/reminder/#{user.unsubscription_token}/") Subscribe
      -else
        li Reminder Welcome email - 
          a(href="../../../unsubscribe/reminder/#{user.unsubscription_token}/") Unsubscribe
      -endif
      
      -if ( user && user.unsubscription.changePassword > 0 )
        li Reminder to change Password - 
          a(href="../../../subscribe/changePassword/#{user.unsubscription_token}/") Subscribe
      -else
        li Reminder to change Password - 
          a(href="../../../unsubscribe/changePassword/#{user.unsubscription_token}/") Unsubscribe
      -endif
      
      li Security notification related to your account
      
-else
  p Something goes wrong. Your request have not been taken into account.
-endif
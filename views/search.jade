doctype html
html(lang='en')
  include ./includes/head.jade
  body
    include ./includes/navbar.jade
    
    .container-fluid
      .row.side-body.hidden-xs
        .col-sm-12
        - if (q)
            h2.fa.fa-2x.fa-search  Search Results for "#{q}"
        - else
            h2.fa.fa-2x.fa-search  Search Results

    .container-fluid
      .row.side-body
        div
          h3 Search Results
          ul
            li
              a(href="#objects") #{objects.length} Object(s)
            li
              a(href="#flows") #{flows.length} Flow(s)
            li
              a(href="#snippets") #{snippets.length} Snippet(s)
            li
              a(href="#dashboards") #{dashboards.length} Dashboard(s)

    .container-fluid#objects
      .row.side-body
        h3.fa-2x.fa.fa-cubes  #{objects.length} Object(s) found
      .side-body.hidden-xs.table-header
        include ./objects/item_header.jade
        
      .row.side-body
        .list-group
        - if (objects.length)
          - for(object in objects) {
            include ./objects/item_list.jade
          - }
        - else
            .list-group-item
              .alert.alert-danger.alert-dismissible(role='alert')
                p No Result in Objects, perhaps you would like to create an 
                  a(href="/objects/add") Object
                  | .

    .container-fluid
      .row.side-body
        .col-sm-12 &nbsp;

    .container-fluid#flows
      .row.side-body
        h3.fa-2x.fa.fa-random  #{flows.length} Flow(s) found
      .side-body.hidden-xs.table-header
        span.col-sm-4
          strong Name
        span.col-sm-2
          strong ID
        span.col-sm-4
          strong Unit
        span.col-sm-2
          strong Permissions
    
      .row.side-body
        .list-group
        - if (flows.length)
          - for(flow in flows) {
            include ./flows/item_list.jade
          - }
        - else
            .list-group-item
              .alert.alert-danger.alert-dismissible(role='alert')
                p No Result in Flows, perhaps you would like to create a 
                  a(href="/flows/add") Flow
                  | .

    .container-fluid
      .row.side-body
        .col-sm-12 &nbsp;

    .container-fluid#snippets
      .row.side-body
        h3.fa-2x.fa.fa-puzzle-piece  #{snippets.length} Snippet(s) found
      .side-body.hidden-xs.table-header
        span.col-sm-2
          strong Icon/Name/Type
        span.col-sm-10
          strong Color/Flows/Id
    
      .row.side-body
        .list-group
        - if (snippets.length)
          - for(snippet in snippets) {
            include ./snippets/item_list.jade
          - }
        - else
            .list-group-item
              .alert.alert-danger.alert-dismissible(role='alert')
                p No Result in Snippets, perhaps you would like to create a 
                  a(href="/snippets") Snippet
                  | .

    .container-fluid
      .row.side-body
        .col-sm-12 &nbsp;

    .container-fluid#dashboards
      .row.side-body
        h3.fa-2x.fa.fa-area-chart  #{dashboards.length} Dashboard(s) found
      .side-body.hidden-xs.table-header
        span.col-sm-8
          strong Name/Description/Id
        span.col-sm-2
          strong Snippets
        span.col-sm-2
          strong Layout
    
      .row.side-body
        .list-group
        - if (dashboards.length)
          - for(dashboard in dashboards) {
            include ./dashboards/item_list.jade
          - }
        - else
            .list-group-item
              .alert.alert-danger.alert-dismissible(role='alert')
                p No Result in Dashboards, perhaps you would like to create a 
                  a(href="/dashboards") Dashboards
                  | .

    .container-fluid
      .row.side-body
        .col-sm-12 &nbsp;

    .container &nbsp;

    include ./includes/foot.jade
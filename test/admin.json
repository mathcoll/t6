{
    "config": {
        "log_path": "./jsonapitest-results.json",
        "defaults": {
            "api_call": {
                "request": {
                    "base_url": "http://127.0.0.1:3000",
                    "headers": {
                        "Accept": "application/json",
                        "Content-Type": "application/json"
                    }
                }
            }
        }
    },
    "data": {"app_version": "v2.0.1"},
    "suites": [
        {
            "name": "t6 Admin Suite",
            "tests": [
                {
                	"name": "can get all DataTypes",
                	"description": "Get all Datatypes",
                    "api_calls": [
                        {
                        	"request": "GET /{{app_version}}/datatypes",
                            "status": 200,
                            "assert": {
        	                    "select": "body.data",
        	                    "size": 9
        	                }
                        }
                    ]
                },
                {
                	"name": "can get all Units",
                	"description": "get all Units",
                    "api_calls": [
                        {
                        	"request": "GET /{{app_version}}/units",
                            "status": 200,
                            "assert": {
        	                    "select": "body.data",
        	                    "size": 123
        	                }
                        }
                    ]
                },
                {
                	"name": "can get some Units using a filtered type 'Volumes'",
                	"description": "get Units using a filter",
                    "api_calls": [
                        {
                        	"request": "GET /{{app_version}}/units?type=Volume",
                            "status": 200,
                            "assert": {
        	                    "select": "body.data",
        	                    "size": 13
        	                }
                        }
                    ]
                },
                {
                	"name": "can get some Units using a filtered type 'Units'",
                	"description": "get Units using a filter",
                    "api_calls": [
                        {
                        	"request": "GET /{{app_version}}/units?type=Température",
                            "status": 200,
                            "assert": {
        	                    "select": "body.data",
        	                    "size": 4
        	                }
                        }
                    ]
                },
                {
                	"name": "can get some Units using a filtered type 'Paper Quantity'",
                	"description": "get Units using a filter",
                    "api_calls": [
                        {
                        	"request": "GET /{{app_version}}/units?type=Paper%20Quantity",
                            "status": 200,
                            "assert": {
        	                    "select": "body.data",
        	                    "size": 1
        	                }
                        }
                    ]
                },
                {
                	"name": "can get t6 server status",
                	"description": "Server Status return basic informations",
                    "api_calls": [
                        {
                        	"request": "GET /{{app_version}}/status",
                            "status": 200,
                            "assert": {
                            	"select": "body.status",
                            	"equal": "running"
        	                }
                        }
                    ]
                },
                {
                	"name": "can get t6 pwa index cards",
                	"description": "List of all cards on the PWA home screen",
                    "api_calls": [
                        {
                        	"request": "GET /{{app_version}}/index",
                            "status": 200,
                            "assert": {
                            	"select": "body.data",
                            	"size": 11
        	                }
                        }
                    ]
                },
                {
                	"name": "can get t6 terms cards",
                	"description": "List of all cards on the PWA Term&Conditions screen",
                    "api_calls": [
                        {
                        	"request": "GET /{{app_version}}/terms",
                            "status": 200,
                            "assert": {
                            	"select": "body.data",
                            	"size": 7
        	                }
                        }
                    ]
                },
                {
                	"name": "can get t6 compatible-devices cards",
                	"description": "List of all cards on the PWA compatible-devices screen",
                    "api_calls": [
                        {
                        	"request": "GET /{{app_version}}/compatible-devices/",
                            "status": 200,
                            "assert": {
                            	"select": "body.data",
                            	"size": 9
        	                }
                        }
                    ]
                }
            ]
        }
    ]
}